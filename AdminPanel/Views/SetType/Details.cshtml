@model SetTypeViewModel
@{
    ViewData["Title"] = "Set Type Details";
}

<div class="container">

    <h1 class="mt-3">'@Model.NameEnglish' Details</h1>

    <form method="post" asp-action="Edit">

        <partial name="DetailsOrDeletePartialView" model="Model" />

        <input type="submit" value="Edit" asp-route-id="@Model.Id" asp-route-view="Edit" class="btn btn-primary mt-3" />
        <a asp-action="Index" class="btn btn-light mt-3">Back To List</a>
    </form>
</div>